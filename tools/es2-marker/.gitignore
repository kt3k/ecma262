# uv-managed venv and Marker scratch output — regenerated, not committed.
# (The *chosen* Marker output is vendored under ecma262/es2/ instead.)
.venv/
# Marker scratch output — the chosen HTML is vendored to ecma262/es2/ instead.
out/
